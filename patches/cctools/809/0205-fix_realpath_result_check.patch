diff -urN a/as/driver.c b/as/driver.c
--- a/as/driver.c	2013-04-02 23:45:03.767222132 +0100
+++ b/as/driver.c	2013-04-02 23:45:03.911220323 +0100
@@ -62,7 +62,7 @@
 	    _NSGetExecutablePath(p, &bufsize);
 	}
 	prefix = realpath(p, resolved_name);
-	if(realpath == NULL)
+if(prefix == NULL)
 	    system_fatal("realpath(3) for %s failed", p);
 	p = rindex(prefix, '/');
 	if(p != NULL)
