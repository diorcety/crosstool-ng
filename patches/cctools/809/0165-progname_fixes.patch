diff -urN a/as/driver.c b/as/driver.c
--- a/as/driver.c	2013-04-02 23:44:51.195380182 +0100
+++ b/as/driver.c	2013-04-02 23:44:51.475376661 +0100
@@ -53,6 +53,9 @@
 	 */
 	bufsize = MAXPATHLEN;
 	p = buf;
+	if(progname != NULL){
+	    strcpy(p, progname);
+	}
 	i = _NSGetExecutablePath(p, &bufsize);
 	if(i == -1){
 	    p = allocate(bufsize);
diff -urN a/libstuff/execute.c b/libstuff/execute.c
--- a/libstuff/execute.c	2013-04-02 23:44:51.147380785 +0100
+++ b/libstuff/execute.c	2013-04-02 23:44:51.427377264 +0100
@@ -161,7 +161,11 @@
 	    p = allocate(bufsize);
 	    _NSGetExecutablePath(p, &bufsize);
 	}
+	strcpy(resolved_name,p);
 	prefix = realpath(p, resolved_name);
+	if(prefix == NULL){
+        prefix = resolved_name;
+	}
 	p = rindex(prefix, '/');
 	if(p != NULL)
 	    p[1] = '\0';
