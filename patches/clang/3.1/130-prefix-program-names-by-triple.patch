diff -urN b/lib/Driver/ToolChain.cpp c/lib/Driver/ToolChain.cpp
--- b/lib/Driver/ToolChain.cpp	2012-03-29 21:53:34.000000000 +0200
+++ c/lib/Driver/ToolChain.cpp	2013-09-08 10:41:33.642685141 +0200
@@ -38,6 +38,17 @@
 }
 
 std::string ToolChain::GetProgramPath(const char *Name, bool WantFile) const {
+  // don't try to prefix clang
+  if(strcmp(Name, "clang")) {
+    // try first with prefixed by triple
+    std::string targetName = getTripleString();
+    targetName += "-";
+    targetName += Name;
+    std::string targetProgram = D.GetProgramPath(targetName.c_str(), *this, WantFile);
+    if(targetProgram != targetName) {
+      return targetProgram;
+    }
+  }
   return D.GetProgramPath(Name, *this, WantFile);
 }
 
